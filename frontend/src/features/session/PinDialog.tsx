import type { Participant } from '../../domain/models'
type Props={participant:Participant;pin:string;busy:boolean;onPin:(pin:string)=>void;onCancel:()=>void;onSubmit:(event:React.FormEvent)=>void}
export function PinDialog({participant,pin,busy,onPin,onCancel,onSubmit}:Props){return <div className="pin-backdrop" role="presentation"><form className="pin-dialog" onSubmit={onSubmit}><div><p className="eyebrow">PIN-код</p><h2>{participant.name}</h2></div><label>6 цифр<input autoFocus inputMode="numeric" maxLength={6} pattern="[0-9]{6}" type="password" value={pin} onChange={e=>onPin(e.target.value.replace(/\D/g,'').slice(0,6))}/></label><div className="pin-actions"><button type="button" onClick={onCancel}>Отмена</button><button disabled={busy||pin.length!==6} type="submit">Войти</button></div></form></div>}
